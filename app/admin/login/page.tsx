'use client'; import { useState } from 'react'; export default function AdminLogin(){ const [pwd,setPwd]=useState(''); async function onLogin(){ const res=await fetch('/api/admin/login',{ method:'POST', headers:{'content-type':'application/json'}, body: JSON.stringify({ password: pwd }) }); if(res.ok) window.location.href='/admin'; else alert('Falsches Passwort'); } return (<div className='max-w-sm mx-auto card'><h1 className='text-xl font-semibold mb-3'>Admin Login</h1><label>Passwort</label><input type='password' value={pwd} onChange={e=>setPwd(e.target.value)} /><button className='btn mt-3 w-full' onClick={onLogin}>Login</button></div>); }