import { NextRequest, NextResponse } from 'next/server'; import { prisma } from '@/lib/db'; export async function POST(req: NextRequest, { params }:{ params:{ id:string } }){ const form=await req.formData(); const trackingCode=String(form.get('trackingCode')||''); await prisma.order.update({ where:{ id: params.id }, data:{ trackingCode } }); return NextResponse.redirect(new URL(`/admin/orders/${params.id}`, req.url)); }