import { NextRequest, NextResponse } from 'next/server'; import { prisma } from '@/lib/db'; export async function POST(req: NextRequest){ const form=await req.formData(); const name=String(form.get('name')||'DHL Paket'); const priceCents=Number(form.get('priceCents')||0); const domesticOnly=String(form.get('domesticOnly')||'true')==='true'; await prisma.shippingOption.create({ data:{ carrier:'DHL_PAKET', name, priceCents, currency:'EUR', domesticOnly, active:true } }); return NextResponse.redirect(new URL('/admin/shipping', req.url)); }