import Link from 'next/link'; import { prisma } from '@/lib/db'; export default async function Page(){ const products=await prisma.product.findMany({orderBy:{sizeMM:'asc'}}); return (<section className='grid gap-8'><div className='space-y-2'><h1 className='text-3xl font-bold'>Products</h1><p className='text-slate-600'>One product per size/shape. Variants (color, epoxy) are inside the product. Images are uploaded when adding to cart — including bulk uploads.</p></div><div className='grid sm:grid-cols-2 gap-6'>{products.map(p=>(<a key={p.slug} href={`/product/${p.slug}`} className='card hover:shadow-sm transition'><div className='aspect-square bg-slate-50 rounded grid place-items-center text-slate-400'>Preview {p.sizeMM}×{p.sizeMM}</div><div className='mt-3'><div className='font-semibold'>{p.name}</div><div className='text-sm text-slate-500'>{p.description}</div></div></a>))}</div></section>); }