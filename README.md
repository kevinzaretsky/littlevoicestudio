<!-- Trigger redeploy -->

# Little Voice Studio — Next.js Starter

Includes:
- `/admin` direct uploads to Cloudinary using unsigned preset
- `/` simple configurator + checkout (Stripe)
- `/products` demo list

## Run locally
```
npm i
cp .env.example .env.local
# edit .env.local with your real keys
npm run dev
```
Then open http://localhost:3000 and http://localhost:3000/admin
